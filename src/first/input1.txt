begin main{
    int a = readk(int);
    int b = readk(int);
    int c = readk(int);
    char ch = 'a';
    string s = "v";
    int max;
    if[ a > b && a > c] then:
        max=a;
    else if[ b > c ] then:
        max=b;
    else:
        max=c;
    console_print(max);
    a = b + c;
    c++;
    int t = -5 - 7;
    end main;
}
